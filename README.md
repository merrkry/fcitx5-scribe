# fcitx5-scribe

An experimental fcitx5 addon that provides building blocks for voice input engines on Linux.

This project is in an early WIP stage.

## Development

To generate `compile_commands.json` for LSP support:

```
mkdir build
cmake -B build
```
